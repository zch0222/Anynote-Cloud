<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anynote.system.mapper.SysApiStatisticsMapper">

    <update id="increaseUsageCount">
        UPDATE
            sys_api_statistics
        SET
            usage_count = usage_count + 1,
            update_time = NOW()
        WHERE
            start_time = #{startTime}
          AND
            end_time = #{endTime}
          AND
            type = #{type}
          AND
            statistics_interval = #{statisticsInterval}
    </update>
</mapper>